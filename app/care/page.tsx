import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export const metadata={title:'Plant Care'};
export default async function Care(){const care=await db.careSheet.findMany({where:{published:true},orderBy:{plantName:'asc'}});return <><section className="pagehero"><div className="container"><div className="eyebrow">Tammy’s plant care library</div><h1>Grow with confidence.</h1><p>Simple, practical care sheets for common houseplants — written for real homes, not greenhouses.</p></div></section><section className="content"><div className="container grid">{care.map(c=><article className="card" key={c.slug}>{c.imageUrl&&<img className="photo" src={c.imageUrl} alt={c.plantName}/>}<div className="cardbody"><span className="pill">Care sheet</span><h3>{c.plantName}</h3>{c.botanical&&<p><i>{c.botanical}</i></p>}<p>{c.summary}</p><p><b>Light:</b> {c.light}</p><p><b>Water:</b> {c.water}</p><p><b>Humidity:</b> {c.humidity}</p><p><b>Soil:</b> {c.soil}</p><p><b>Feeding:</b> {c.feeding}</p><p><b>Temperature:</b> {c.temperature}</p>{c.petSafety&&<p><b>Pet safety:</b> {c.petSafety}</p>}<p style={{borderTop:'1px solid var(--line)',paddingTop:14}}><b>Tammy’s tip:</b> {c.tips}</p></div></article>)}</div></section></>}
