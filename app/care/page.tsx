import {care} from '@/lib/catalog';
export const metadata={title:'Plant Care'};
export default function Care(){return <><section className="pagehero"><div className="container"><div className="eyebrow">Tammy’s plant care library</div><h1>Grow with confidence.</h1><p>Simple, practical care sheets for common houseplants — written for real homes, not greenhouses.</p></div></section><section className="content"><div className="container grid">{care.map(c=><article className="card" key={c.slug}><div className="cardbody"><span className="pill">Care sheet</span><h3>{c.name}</h3><p><b>Light:</b> {c.light}</p><p><b>Water:</b> {c.water}</p><p><b>Humidity:</b> {c.humidity}</p><p><b>Pet safety:</b> {c.pet}</p><p style={{borderTop:'1px solid var(--line)',paddingTop:14}}><b>Tammy’s tip:</b> {c.tip}</p></div></article>)}</div></section></>}
