import {catalog} from '@/lib/catalog';
export const metadata={title:'Shop'};
export default function Shop(){return <><section className="pagehero"><div className="container"><div className="eyebrow">Plants • Teas • Botanicals</div><h1>Shop The Hillside</h1><p>Hand-selected plants and small-batch goods from Tammy’s garden-inspired collection.</p></div></section><section className="content"><div className="container"><div className="toolbar"><b>{catalog.length} products</b><span className="pill">Starter catalog — manage from Admin</span></div><div className="grid">{catalog.map(p=><article className="card" key={p.id}><img className="photo" src={p.image} alt={p.name}/><div className="cardbody"><span className="pill">{p.type}</span><h3>{p.name}</h3><p>{p.description}</p><p className="price">${p.price.toFixed(2)}</p><button className="btn">Add to cart</button></div></article>)}</div></div></section></>}
