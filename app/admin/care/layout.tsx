// The care manager renders the same card and form styles as the public library.
import '../../care-library.css';

export default function AdminCareLayout({ children }: { children: React.ReactNode }) {
  return children;
}
